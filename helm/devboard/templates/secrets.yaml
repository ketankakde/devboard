apiVersion: v1
kind: Secret
metadata:
  name: devboard-secrets
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  mongo-root-password: {{ .Values.mongodb.rootPassword | quote }}
  jwt-secret: {{ .Values.auth.jwtSecret | quote }}
