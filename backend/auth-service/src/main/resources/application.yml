server:
  port: 8081
spring:
  application:
    name: auth-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/devboard}
app:
  jwt-secret: ${JWT_SECRET:development-secret-must-be-at-least-32-characters-long}
